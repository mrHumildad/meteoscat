# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
meteokat/venv
node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
MUSHROOM_APP_PLAN.md

basura

# Raw scraped XEMA payloads (rebuilt daily by CI; too big for git). The
# committed artifact is the aggregated public/logic/daily/ shards instead.
dades_*.json
full_dades.json

# Raw ICGC geology download (155 MB zip / 283 MB gpkg) — source for
# build_lithology.py; the committed artifact is public/logic/litho_grid.json.
meteokat/raw
